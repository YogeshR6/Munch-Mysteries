<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=munch.title%></title>
  </head>
  <body>
    <h1>Title: <%= munch.title %></h1>
    <h3>Description: <%= munch.description %></h3>
    <h3>Location: <%= munch.location %></h3>
    <section>
      <a href="/munches/<%= munch._id %>/edit">Edit</a>
    </section>
    <section>
      <a href="/munches">Go to all Munches</a>
    </section>
    <section>
      <form action="/munches/<%= munch._id %>?_method=DELETE" method="POST">
        <button>Delete</button>
      </form>
    </section>
  </body>
</html>
